name: just_audio_background
description: Vendored fork — stop control order + compatible SDK (upstream 0.0.1-beta.17).
publish_to: none
version: 0.0.1-beta.17

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  just_audio_platform_interface: ^4.5.0
  audio_service: ^0.18.18
  audio_session: ">=0.1.24 <0.3.0"
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  meta: ^1.3.0
  rxdart: ">=0.26.0 <0.29.0"
  synchronized: ^3.3.0+3

dev_dependencies:
  flutter_lints: ^5.0.0
